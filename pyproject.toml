[project]
name = "meshpilot-social-media-agent"
version = "0.1.0"
description = "Autonomous social media identity agent — mines shipped artifacts, generates short-form video with AI models, publishes to YouTube Shorts / X / Instagram Reels, and manages ORM within hard guardrails."
requires-python = ">=3.11"
readme = "README.md"
license = { file = "LICENSE" }
keywords = [
  "social-media-agent", "ai-video-generation", "youtube-shorts", "orm",
  "langgraph", "kling", "short-form-video", "autonomous-agent",
  "content-automation", "brand-identity",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Internet :: WWW/HTTP",
  "Topic :: Multimedia :: Video",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
  "Framework :: AsyncIO",
]

dependencies = [
  # Web / server
  # `[standard]` pulls the `fastapi` CLI (+ uvicorn[standard]); FastAPI Cloud
  # launches the app via `fastapi run`, which requires it. Bare `fastapi`
  # crash-loops the runtime with "please install fastapi[standard]".
  "fastapi[standard]>=0.115",
  "uvicorn[standard]>=0.32",
  "httpx>=0.27",
  "pydantic>=2.9",
  "pydantic-settings>=2.6",
  "python-dotenv>=1.0",
  # Observability — Logfire (FastAPI Cloud injects LOGFIRE_TOKEN when the
  # integration is connected in the dashboard).
  "logfire[fastapi]>=2",
  # Observability — Sentry (errors + tracing). Auto-enables the FastAPI
  # integration; init'd in main.py behind SENTRY_DSN.
  "sentry-sdk>=2",
  # Agent stack
  "langgraph>=0.2",
  "langchain-core>=0.3",
  "litellm>=1.52",
  # LLM providers — OpenRouter only. The Anthropic SDK was never imported (the loop has always
  # spoken raw HTTP to OpenRouter); it sat in the manifest as an unused dependency.
  "google-genai>=0.3",
  # MCP client — the agent connects to external tools' MCP servers (AGENT-MCP)
  "mcp>=1.2",
  # Native deterministic image editing (resize/crop/text/format) — complements AI media gen
  "pillow>=10.2",
  # Higgsfield — third media provider (image/video/3D/audio gen via its SDK)
  "higgsfield-client>=0.1",
  # Database
  "sqlmodel>=0.0.21",
  "asyncpg>=0.30",
  # SQLAlchemy's async engine requires greenlet; it is not auto-installed on
  # every arch (e.g. macOS arm64), so pin it explicitly for local + cloud.
  "greenlet>=3.0",
  # Video assembly
  "ffmpeg-python>=0.2.0",
  # Utilities
  "structlog>=24.4",
  "tenacity>=9.0",
  "aiofiles>=24.1",
  "cryptography>=43.0",
  # Google APIs (Drive for drive_footage content source; YouTube Data for
  # uploads — moved from phase2 into core because the drive_footage path
  # needs google-api-python-client at boot)
  "google-api-python-client>=2.150",
  "google-auth>=2.35",
  "google-auth-httplib2>=0.2",
  # Replicate — image generation via recraft-v3 / FLUX for LinkedIn image posts.
  "replicate>=1.0",
  # fal.ai — primary image generation provider (FLUX.1-schnell, fast + cheap).
  "fal-client>=0.5",
  # img2pdf — lossless PNG→PDF for LinkedIn carousel document posts.
  "img2pdf>=0.5",
  "croniter>=6.2.4",
  "resend>=2.42.0",
]

[project.optional-dependencies]
phase2 = [
  "tweepy>=4.14",
  "google-auth-oauthlib>=1.2",
]
dev = [
  "pytest>=8.3",
  "pytest-asyncio>=0.24",
  "aiosqlite>=0.20",   # in-memory async DB for tests — never used at runtime
  "ruff>=0.7",
  "mypy>=1.13",
  "pypdf>=5.0",        # test-only: proves the rendered CV has a machine-readable text layer
]

# NOTE: [project.urls] MUST come after `dependencies` and
# `[project.optional-dependencies]`. Otherwise, per the TOML spec, the
# unqualified `dependencies` key would be parsed as `project.urls.dependencies`,
# which newer Hatchling versions (>= ~1.24) reject with:
#   TypeError: URL `dependencies` of field `project.urls` must be a string
[project.urls]
Homepage = "https://meshpilot.app"
Repository = "https://github.com/Nuraveda-Labs/meshpilot-digital-marketing-agent"
Issues = "https://github.com/Nuraveda-Labs/meshpilot-digital-marketing-agent/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/meshpilot"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "B", "UP", "ASYNC"]
ignore = [
    "E501",     # line length — handled by formatters, not lint
    # The ASYNC rules below warn when async functions touch the blocking
    # pathlib / open() APIs. Inside the agent the affected spots are
    # Telegram preview paths + tiny stat() / exists() checks; switching the
    # whole codebase to aiofiles for those is more churn than reward.
    "ASYNC230",
    "ASYNC240",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
# Keep the suite honest (#107): a NEW deprecation raised from our own code fails the run, so it can't
# hide a real regression. Scoped to first-party — third-party library deprecations stay warnings.
filterwarnings = [
    "error::DeprecationWarning:meshpilot",
]
testpaths = ["tests"]
